
import styles from './style.module.scss';

const Header = () => {
    return (
        <header>
            <div className={styles.header}>Header</div>
        </header>
    );
}

export default Header;